#include "interface_CLL.h"
using namespace std;
int main()
{
    interface program;
    program.manager();
/*
    generic_communication * a = new line("a", "Zack");
    a->add_contact("A", "Zack");
    a->add_contact("A", "Z");
    a->add_contact("d", "Salah");
    a->add_contact("b", "oa`");
    a->add_contact("c", "Salah");
    a->add_contact("c", "Sh");
    a->add_contact("e", "Salah");
    a->add_contact("F", "Salah");
    a->add_contact("G", "Ghassan");
    a->send_message("hello3", "A", "Zack");
    a->send_message("hello3", "A", "Zack");
    a->send_message("hello3", "A", "Zack");
    a->send_message("hello3", "A", "Zack");
    a->send_message("hello3", "A", "Zack");
    a->send_message("hello3", "A", "Zack");
    a->remove_inContact("A", "Z");
    a->remove_all_last("a");
//    a.retrieve("Zack");
    a->display();
    //a->retrieve_inContact("A", "Zack");
    a->retrieve_all_last("c");
    facebook * ptr;
    snapchat * sptr;
    phone * pptr;
    line * lptr;
    ptr = dynamic_cast<facebook*>(a);
    if(ptr)
    {
        ptr->poke_someone("lala", "fofo");
        ptr->poke_someone("F", "Salah");
        ptr->poke_someone("e", "Salah");
        ptr->poke_someone("F", "Salah");
        //ptr->display_poke_list();
        ptr->delete_poked("F","Salah");
        ptr->retrieve_poked("F", "Salah");
    }
    sptr = dynamic_cast<snapchat*>(a);
    if(sptr)
    {
        sptr->add_story("I am a man with no soul. some call a devil, some call me a lucifer's seed. Alas, I neither sympithize nor give a shit with those cunt fucking morons");
        sptr->display_story();
    }
    pptr = dynamic_cast<phone*>(a);
    if(pptr)
    {
        long unsigned int number = 9712588438;
        pptr->update_phone_number(number);
        pptr->display_phone_number();
    }
    lptr = dynamic_cast<line*>(a);
    if(lptr)
    {
        lptr->send_emojis("helloo","F", "Salah");
    }
    a->retrieve_inContact("F", "Salah");
    //a->remove_contact("Salah");
    //a->display();
    delete a;
    */
}
